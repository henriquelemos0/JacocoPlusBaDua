<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>br.usp.each.saeg.badua</groupId>
  <artifactId>example</artifactId>
  <packaging>jar</packaging>
  <version>1.0</version>
  <name>example</name>
  <url>http://maven.apache.org</url>
  
  <properties>
    <badua>lib/ba-dua-0.0.2-jar-with-dependencies.jar</badua>
  </properties>


  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.8.1</version>
      <scope>test</scope>
    </dependency>
  </dependencies>


  <build>
    <plugins>
      <!-- this configures the surefire plugin to run your tests with the javaagent enabled -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
            <forkMode>pertest</forkMode>
            <argLine>-javaagent:${project.basedir}/lib/ba-dua-0.0.2-jar-with-dependencies.jar</argLine>
            <!--<argLine>-Doutput.file=coverage.ser</argLine>-->
            <workingDirectory>${project.basedir}</workingDirectory>
          </configuration> 
       <!--<executions>
          <execution>
            <goals>
              <goal>test</goal>
            </goals>
            <configuration>
              <forkMode>pertest</forkMode>
              <argLine>-javaagent:${project.basedir}/lib/ba-dua-0.0.2-jar-with-dependencies.jar</argLine>
              <properties>
              	<property>
              		<name>-Doutput.file</name>
              		<value>coverage.ser</value>
              	</property>
              </properties>
              <workingDirectory>${project.basedir}</workingDirectory>
            </configuration>
          </execution>
        </executions> -->
      </plugin>
  </plugins>
  </build>
</project>
